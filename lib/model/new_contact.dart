import 'dart:async';

class NewContact{
  String ? nom;
  String ? prenom;
  String ? pseudo;
  String ? tel;
  String ? mail;
  NewContact({required this.nom, required this.prenom, required this.pseudo, required this.mail, required this.tel});

}